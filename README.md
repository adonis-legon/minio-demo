# Demo Project for Minio integration

## Run Locally for Minio

`$ mvn spring-boot:run -Dspring-boot.run.profiles=minio`

## Run Locally for AWS S3

`$ mvn spring-boot:run -Dspring-boot.run.profiles=s3`